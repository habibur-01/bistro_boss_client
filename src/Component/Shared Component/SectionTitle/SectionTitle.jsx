

const SectionTitle = ({heading, subHeading}) => {
    return (
        <div>
            <h1>{heading}</h1>
            <h3>{subHeading}</h3>
            
        </div>
    );
};

export default SectionTitle;